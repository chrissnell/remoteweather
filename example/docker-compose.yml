version: '2'
services:
  gopherwx:
    network_mode: "host"
    image: chrissnell/gopherwx:v4.0
    environment:
    - GOPHERWX_CONFIG=/config/config.yaml
    volumes:
     - /etc/gopherwx:/config
     - /etc/letsencrypt:/tls
